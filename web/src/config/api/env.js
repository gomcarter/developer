export const DOMAIN = window.location.hostname.split('.').slice(-2).join('.')
/* eslint-disable*/
export const LOCAL_URL = '.test.com'

export const BASE_URL = `//base.${DOMAIN}/`

export const IMAGE_SERVER_URL = `//pic.${DOMAIN}/`

// export const INSERV_URL = `//d.${DOMAIN}/`
export const INSERV_URL = `/`
